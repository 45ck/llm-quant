# Research Spec: tlt-spy-inverse-lead-lag
strategy_slug: "tlt-spy-inverse-lead-lag"
strategy_type: "lead_lag"

frozen: true
frozen_at: "2026-03-26T00:00:00Z"
frozen_by: "claude-sonnet-4-6"
frozen_hash: "placeholder-computed-on-freeze"
modification_lock: >
  This spec is FROZEN. Parameter changes require a new spec version.

parameters:
  leader_symbol: "TLT"
  follower_symbol: "SPY"
  lag_days: 3
  signal_window: 3
  entry_threshold: 0.008
  exit_threshold: -0.003
  target_weight: 0.90
  inverse: true
  rebalance_frequency_days: 1

cost_model:
  spread_bps: 5.0
  flat_slippage_bps: 2.0
  slippage_volatility_factor: 0.1
  commission_per_share: 0.0
  min_commission: 0.0

parameter_rationale:
  lag_days: "From hypothesis. TLT risk-off sentiment propagates to equity 3 days later. Not optimized."
  signal_window: "3-day window captures short-term directional momentum in TLT without over-fitting."
  entry_threshold: "0.8% cumulative TLT return threshold. Set higher than HYG to filter noise in bond market. Pre-specified."
  exit_threshold: "Exit on +0.3% TLT rise (inverse exit). Pre-specified, not tuned on in-sample data."
  inverse: "True -- TLT up means risk-off, predict SPY down; TLT down means risk-on, predict SPY up."

backtest_spec:
  symbols: ["TLT", "SPY"]
  years: 5
  initial_capital: 100000
  warmup_days: 30

benchmark:
  symbols:
    SPY: 1.00

family: "lead_lag_tlt_spy"
family_trial_number: 1
family_prior_trials: 0

created_at: "2026-03-26"
updated_at: "2026-03-26"
